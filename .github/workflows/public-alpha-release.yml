name: Public Alpha Release

on:
  schedule:
    - cron: "*/5 * * * *"
  push:
    branches:
      - main
    paths:
      - "LICENSE"
      - ".github/workflows/public-alpha-release.yml"
      - "docs/release-notes-0.1.0-alpha.1.md"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: public-alpha-v0.1.0-alpha.1
  cancel-in-progress: false

jobs:
  release:
    name: Publish verified CI artifacts
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-22.04
    timeout-minutes: 15
    permissions:
      actions: read
      contents: write
    env:
      RELEASE_TAG: v0.1.0-alpha.1
      RELEASE_TITLE: Tpf2 Mod Studio 0.1.0 Public Alpha 1
      SOURCE_RUN_ID: "30484600653"
      SOURCE_COMMIT: 7cc504046a730860c77b46c340cb0fe18f5ce5d4
      LINUX_ARTIFACT_ID: "8737216482"
      LINUX_ARCHIVE_SHA256: 5625ab26ab04a4e7d0b887dd83502cce302733f6979992a02f79d0ae0e6284ed
      WINDOWS_ARTIFACT_ID: "8737200166"
      WINDOWS_ARCHIVE_SHA256: 1cf460c4eeee24572dcdb252a44068924b5818a2961117ad78c80d0be5764d98

    steps:
      - name: Check out release metadata
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Require approved GPL-3.0-only license
        shell: bash
        run: |
          if [ ! -s LICENSE ]; then
            echo "LICENSE is missing."
            exit 1
          fi
          grep -q "GNU GENERAL PUBLIC LICENSE" LICENSE
          grep -q "Version 3, 29 June 2007" LICENSE
          grep -q '"license": "GPL-3.0-only"' package.json

      - name: Reuse an existing completed release
        id: existing
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if gh release view "$RELEASE_TAG" >/dev/null 2>&1; then
            echo "release_exists=true" >> "$GITHUB_OUTPUT"
            echo "Release $RELEASE_TAG already exists; no replacement is required."
          else
            echo "release_exists=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Download verified CI artifact archives
        if: steps.existing.outputs.release_exists != 'true'
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          mkdir -p release-assets/linux release-assets/windows
          gh api \
            -H "Accept: application/vnd.github+json" \
            "/repos/$GITHUB_REPOSITORY/actions/artifacts/$LINUX_ARTIFACT_ID/zip" \
            > "$RUNNER_TEMP/linux-artifact.zip"
          gh api \
            -H "Accept: application/vnd.github+json" \
            "/repos/$GITHUB_REPOSITORY/actions/artifacts/$WINDOWS_ARTIFACT_ID/zip" \
            > "$RUNNER_TEMP/windows-artifact.zip"

      - name: Verify artifact provenance and unpack packages
        if: steps.existing.outputs.release_exists != 'true'
        shell: bash
        run: |
          printf '%s  %s\n' \
            "$LINUX_ARCHIVE_SHA256" \
            "$RUNNER_TEMP/linux-artifact.zip" \
            | sha256sum -c -
          printf '%s  %s\n' \
            "$WINDOWS_ARCHIVE_SHA256" \
            "$RUNNER_TEMP/windows-artifact.zip" \
            | sha256sum -c -
          unzip -q "$RUNNER_TEMP/linux-artifact.zip" -d release-assets/linux
          unzip -q "$RUNNER_TEMP/windows-artifact.zip" -d release-assets/windows

      - name: Verify package set and create checksums
        if: steps.existing.outputs.release_exists != 'true'
        shell: bash
        run: |
          for pattern in "*.msi" "*.exe" "*.AppImage" "*.deb" "*.rpm"; do
            if ! find release-assets -type f -name "$pattern" -print -quit | grep -q .; then
              echo "Required release package is missing: $pattern"
              exit 1
            fi
          done
          : > release-assets/SHA256SUMS.txt
          while IFS= read -r -d '' package; do
            checksum="$(sha256sum "$package" | cut -d ' ' -f 1)"
            printf '%s  %s\n' \
              "$checksum" \
              "$(basename "$package")" \
              >> release-assets/SHA256SUMS.txt
          done < <(
            find release-assets -type f ! -name SHA256SUMS.txt -print0 | sort -z
          )
          cat release-assets/SHA256SUMS.txt

      - name: Create immutable Public Alpha pre-release
        if: steps.existing.outputs.release_exists != 'true'
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          mapfile -d '' assets < <(
            find release-assets -type f ! -name SHA256SUMS.txt -print0
          )
          gh release create "$RELEASE_TAG" \
            "${assets[@]}" \
            release-assets/SHA256SUMS.txt \
            --target "$GITHUB_SHA" \
            --title "$RELEASE_TITLE" \
            --notes-file docs/release-notes-0.1.0-alpha.1.md \
            --prerelease

      - name: Record release provenance
        shell: bash
        run: |
          echo "Release: $RELEASE_TAG"
          echo "Verified source run: $SOURCE_RUN_ID"
          echo "Verified source commit: $SOURCE_COMMIT"
